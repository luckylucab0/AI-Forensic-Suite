# Artefaktkatalog

[English](ARTIFACTS.md) | Deutsch

<!-- Generated file. Do not edit. -->

Erzeugt aus catalog/ durch scripts/gen_artifact_docs.py. Nicht von Hand bearbeiten: die CI erzeugt diese Datei neu und schlägt fehl, wenn sie abweicht.

78 Artefakte über 1 Agent(en), davon 74 auf einer abgerufenen Herstellerquelle beruhend.

Als unbestätigt markierte Einträge werden trotzdem gesammelt, aber keine Herstellerquelle bestätigt den Pfad. Das Fehlen eines solchen Artefakts ist daher kein Beweis dafür, dass der Agent nicht genutzt wurde, sondern unklar.

## Claude Code

Vendor: Anthropic

[EN] Anthropic's terminal coding agent, also reachable from the desktop app, VS Code, JetBrains IDEs, the Agent SDK and CI. It keeps by far the richest on-disk record of any agent covered here: full transcripts, every prompt typed, pre-edit file snapshots, subagent activity, permission decisions and effective policy.

<https://code.claude.com/docs/en/claude-directory>, <https://code.claude.com/docs/en/settings>, <https://code.claude.com/docs/en/sessions>, <https://code.claude.com/docs/en/iam>

### Verschiebende Variablen

- `CLAUDE_CONFIG_DIR`: [EN] Moves the whole configuration directory away from ~/.claude, taking settings, transcripts, prompt history and plugins with it. A collection keyed on ~/.claude alone finds nothing on such a host, which is indistinguishable from the agent never having run. Read the variable from the shell profile and from process environment evidence before concluding an agent was absent.
- `CLAUDE_CODE_SKIP_PROMPT_HISTORY`: [EN] Suppresses writing to history.jsonl. Its presence in a shell profile is itself a finding, since prompt history normally outlives the transcripts.

### live_only

**Nur am laufenden System zu holen.** Diese Artefakte existieren nur, solange der Agent oder die Sitzung läuft, oder werden beim saubern Herunterfahren zerstört. Aus einem abgeschalteten Abbild sind sie nicht wiederherstellbar. Läuft das Endgerät noch, dann hier anfangen.

| Kennung | Name | Kategorie | Betriebssysteme | Pfade | Format | Sensitivität | Aufbewahrung | Status | Quelle |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `claude_code.sessions_dir` | sessions dir | log | Linux, macOS, Windows | `%USERPROFILE%\.claude\sessions\*`<br>`~/.claude/sessions/`<br>`~/.claude/sessions/*` | json | normal | [EN] NOT part of the age-based sweep. One small file per RUNNING session; Claude Code removes each file when its session exits and clears crash leftovers on the next launch.  NOT part of the age-based sweep. One small file per RUNNING session; each file is removed when its session exits, and crash leftovers are cleared on the next launch. Live-response only - a file here means either a session running right now or one that died uncleanly (changelog v2.1.246 fixed headless sessions failing to clean up stale entries). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.shell_snapshots` | shell snapshots | shell_history | Linux, macOS, Windows | `%USERPROFILE%\.claude\shell-snapshots\*`<br>`/tmp/claude-shell-snapshot*`<br>`~/.claude/shell-snapshots/`<br>`~/.claude/shell-snapshots/*` | text | normal | [EN] Removed on CLEAN EXIT - a snapshot surviving on disk generally means that session crashed or was killed. The sweep clears whatever is left after a crash (brought under cleanupPeriodDays in v2.1.117). Explicitly NOT project-scoped and explicitly left alone by `claude project purge`, which even warns about it - so this directory survives the main anti-forensic command.  HIGHLY VOLATILE: removed on clean exit, so files are normally present only while a session runs or after a CRASH. The retention sweep clears any left after a crash. Explicitly left alone by `claude project purge` (including --all) because it is not project-scoped, and warned about in the purge plan. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |

### first

**Zuerst sichern.** Rotieren oder verfallen aggressiv, nach Anzahl oder bei jedem Aufräumlauf statt nach einer bequemen Frist. Einige davon zerstört die Benutzerin schon dadurch, dass sie eine weitere Sitzung startet.

| Kennung | Name | Kategorie | Betriebssysteme | Pfade | Format | Sensitivität | Aufbewahrung | Status | Quelle |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `claude_code.config_backups` | config backups | config | Linux, macOS, Windows | `%USERPROFILE%\.claude\backups\*`<br>`~/.claude/backups/`<br>`~/.claude/backups/*`<br>`~/.claude/backups/.claude.json.corrupted.<timestamp>` | json | normal | [EN] Only the FIVE newest copies are kept, plus a copy of any version Claude Code could not parse - so this rotates by count, not by age, and a busy machine loses history fast. Brought under the cleanupPeriodDays sweep in v2.1.117. Explicitly left alone by `claude project purge`, which warns that 'backups/ may still contain this project entry in old .claude.json snapshots'.  Claude Code keeps only the FIVE NEWEST versions of ~/.claude.json, plus a copy of any version it could not parse. Explicitly left alone by `claude project purge` because it is not project-scoped. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.credentials` | credentials | credentials | Linux, macOS, Windows | `$CLAUDE_CONFIG_DIR/.credentials.json`<br>`%USERPROFILE%\.claude\.credentials.json`<br>`~/.claude/.credentials.json` | json | secret | [EN] Listed among the state files to KEEP - not swept. Present only when credentials are stored in a file rather than the OS keychain; the vendor authentication page is the authority on where credentials live per platform (not fetched in this pass).  Managed by /login and /logout; /logout removes and revokes all stored credentials including saved MCP server logins and plugin sensitive values. On macOS, once the Keychain becomes writable again Claude Code moves credentials back into the Keychain the next time it writes one. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.feedback_bundles` | feedback bundles | transcript | Linux, macOS, Windows | `%USERPROFILE%\.claude\feedback-bundles\*`<br>`~/.claude/feedback-bundles/`<br>`~/.claude/feedback-bundles/*`<br>`~/.claude/feedback/drafts/` | binary | normal | [EN] Swept under cleanupPeriodDays (default 30 days).  feedback-bundles/ is swept after cleanupPeriodDays. feedback/drafts/ is swept after cleanupPeriodDays OR 30 days, whichever is SHORTER, and at the 10-draft limit the oldest draft is deleted to make room. | bestätigt | [official](https://code.claude.com/docs/en/data-usage.md) |
| `claude_code.feedback_drafts` | feedback drafts | log | macOS, Windows, Linux | `~/.claude/feedback/drafts/`<br>`~/.claude/feedback/drafts/*`<br>`%USERPROFILE%\.claude\feedback\drafts\*` | json | normal | [EN] Double-bounded: swept after cleanupPeriodDays OR 30 days, whichever is SHORTER, and the queue is capped at 10 drafts - reaching the cap deletes the oldest draft. So this is a rolling window of at most 10 items. | bestätigt | [official](https://code.claude.com/docs/en/tools-reference.md) |
| `claude_code.file_history_snapshots` | file history snapshots | file_snapshot | macOS, Windows, Linux | `~/.claude/file-history/<session-id>/`<br>`~/.claude/file-history/*/*`<br>`%USERPROFILE%\.claude\file-history\*\*` | binary | normal | [EN] Aggressively pruned. Only the 100 most recent checkpoints per session are retained; when an older checkpoint is discarded, the snapshot files no remaining checkpoint references are deleted, EXCEPT each file's first snapshot (kept as the VS Code extension's session-diff baseline). The whole directory is then swept under cleanupPeriodDays, ~30 days after the session last saved a snapshot. v2.1.50 capped snapshots to bound memory; v2.1.208 began pruning superseded file-history backups to bound disk. `claude project purge` deletes the per-session entries. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.image_cache` | image cache | cache | macOS, Windows, Linux | `~/.claude/image-cache/<session-id>/`<br>`~/.claude/image-cache/*/*`<br>`%USERPROFILE%\.claude\image-cache\*\*` | binary | normal | [EN] MOST VOLATILE artifact in the tree: on each sweep Claude Code removes the directories of ALL OTHER sessions, whatever their age - i.e. starting any new session can wipe every other session's attached images immediately, not after 30 days. Collect this first, and prefer live acquisition. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.macos_keychain_credentials` | macos keychain credentials | credentials | macOS | `~/Library/Keychains/login.keychain-db` | binary | secret | [EN] Removed and revoked by /logout. Rewritten on each credential refresh (each rewrite re-arms the ACL prompt behaviour described below). | bestätigt | [official](https://code.claude.com/docs/en/troubleshoot-install.md) |
| `claude_code.policy_limits` | policy limits | config | macOS, Windows, Linux | `~/.claude/policy-limits.json` | json | normal | [EN] DELETED ON LOGOUT. Not swept by age. Refreshed automatically. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.subagent_transcripts` | subagent transcripts | transcript | macOS, Windows, Linux | `~/.claude/projects/<project>/<session-id>/subagents/agent-<agentId>.jsonl`<br>`~/.claude/projects/<project>/<session-id>/subagents/agent-<agentId>.meta.json`<br>`~/.claude/projects/*/*/subagents/*.jsonl`<br>`%USERPROFILE%\.claude\projects\*\*\subagents\*.jsonl` | jsonl | normal | [EN] Removed with the parent session transcript when it ages out under cleanupPeriodDays (default 30 days). Unaffected by main-conversation compaction - they live in separate files, so a subagent transcript can survive content that was compacted out of the trunk. | bestätigt | [official](https://code.claude.com/docs/en/sub-agents.md) |
| `claude_code.tool_result_spills` | tool result spills | transcript | macOS, Windows, Linux | `~/.claude/projects/<project>/<session-id>/tool-results/`<br>`~/.claude/projects/*/*/tool-results/*`<br>`%USERPROFILE%\.claude\projects\*\*\tool-results\*` | text | normal | [EN] Swept with the session under cleanupPeriodDays (default 30 days). Changelog v2.1.83 fixed 'tool result files never being cleaned up, ignoring the cleanupPeriodDays setting' - on evidence from before v2.1.83 expect spill files far older than the retention window to survive. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.transcripts` | transcripts | transcript | macOS, Windows, Linux | `~/.claude/projects/<project>/<session-id>.jsonl`<br>`~/.claude/projects/*/*.jsonl`<br>`%USERPROFILE%\.claude\projects\*\*.jsonl`<br>`$CLAUDE_CONFIG_DIR/projects/*/*.jsonl` | jsonl | normal | [EN] Deleted by a startup retention sweep once older than `cleanupPeriodDays` (default 30 days, minimum 1; `0` is rejected with a validation error since v2.1.89). Sweep is skipped entirely in bare mode (`claude -p --bare`) and paused when the retention period cannot be safely determined. Transcripts of sessions started or last continued in the desktop/Cowork app are kept at ANY age unless `desktopSessionCleanupPeriodDays` is set (v2.1.248+). `claude project purge <path>` deletes one project's transcripts on demand. | bestätigt | [official](https://code.claude.com/docs/en/sessions.md) |
| `claude_code.transcripts_set_aside` | transcripts set aside | transcript | macOS, Windows, Linux | `~/.claude/projects/<project>/<session-id>.orphaned-<timestamp>-<suffix>.jsonl`<br>`~/.claude/projects/<project>/<session-id>.jsonl.superseded-<timestamp>`<br>`~/.claude/projects/*/*.orphaned-*.jsonl`<br>`~/.claude/projects/*/*.jsonl.superseded-*`<br>`%USERPROFILE%\.claude\projects\*\*.jsonl.superseded-*` | jsonl | normal | [EN] Same age-based sweep as the live transcript (cleanupPeriodDays, default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.workflow_runs` | workflow runs | transcript | macOS, Windows, Linux | `~/.claude/projects/<project>/<session-id>/subagents/workflows/<runId>/journal.jsonl`<br>`~/.claude/projects/<project>/<session-id>/subagents/workflows/<runId>/agent-<agentId>.jsonl`<br>`~/.claude/projects/<project>/<session-id>/subagents/workflows/<runId>/agent-<agentId>.meta.json`<br>`~/.claude/projects/<project>/<session-id>/workflows/<runId>.json`<br>`~/.claude/projects/*/*/subagents/workflows/*/*.jsonl` | jsonl | normal | [EN] Lives under the session directory, so swept with the session under cleanupPeriodDays (default 30 days). | **unbestätigt** | [community](https://raw.githubusercontent.com/daaain/claude-code-log/main/claude_code_log/workflow.py) |

### normal

**Normal sichern.** Unterliegen der üblichen Aufbewahrungsfrist des Agenten, die bei mehreren Agenten standardmässig 30 Tage beträgt.

| Kennung | Name | Kategorie | Betriebssysteme | Pfade | Format | Sensitivität | Aufbewahrung | Status | Quelle |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `claude_code.anthropic_active_config` | anthropic active config | config | macOS, Linux, Windows | `~/.config/anthropic/active_config`<br>`%APPDATA%\Anthropic\active_config`<br>`$ANTHROPIC_CONFIG_DIR/active_config` | text | normal | [EN] Overwritten by `ant profile activate <name>`. | bestätigt | [official](https://platform.claude.com/docs/en/manage-claude/wif-reference) |
| `claude_code.anthropic_profile_configs` | anthropic profile configs | config | macOS, Linux, Windows | `~/.config/anthropic/configs/<profile>.json`<br>`%APPDATA%\Anthropic\configs\<profile>.json`<br>`$ANTHROPIC_CONFIG_DIR/configs/<profile>.json` | json | normal | [EN] Written by `ant auth login`, by `ant profile activate`, or by a Claude Code Console sign-in without an API key. Explicitly described as safe to commit or bake into a container image, so it may also appear in repositories and images. | bestätigt | [official](https://platform.claude.com/docs/en/manage-claude/wif-reference) |
| `claude_code.anthropic_profile_credentials` | anthropic profile credentials | credentials | macOS, Linux, Windows | `~/.config/anthropic/credentials/<profile>.json`<br>`%APPDATA%\Anthropic\credentials\<profile>.json`<br>`$ANTHROPIC_CONFIG_DIR/credentials/<profile>.json` | json | secret | [EN] Rewritten on each token exchange/refresh. Minted access tokens are short-lived (token_lifetime_seconds 60-86400, default 3600), so a captured access_token is usually expired; the refresh_token is the durable secret. | bestätigt | [official](https://platform.claude.com/docs/en/manage-claude/wif-reference) |
| `claude_code.changelog_cache` | changelog cache | install_evidence | Linux, macOS, Windows | `%USERPROFILE%\.claude\cache\changelog.md`<br>`~/.claude/cache/changelog.md` | markdown | normal | [EN] Not age-swept; refreshed in the background, so its content tracks the latest published release rather than the installed one.  NOT age-swept - kept until deleted. Refreshed in the background. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.daemon_state` | daemon state | log | Linux, macOS, Windows | `%USERPROFILE%\.claude\daemon.log`<br>`%USERPROFILE%\.claude\daemon\roster.json`<br>`~/.claude/daemon.lock`<br>`~/.claude/daemon.log`<br>`~/.claude/daemon/roster.json` | json | normal | [EN] `daemon/` is listed among the state files to keep. `daemon.lock` is a PID lock that goes stale across reboots and is replaced (v2.1.257) or must be deleted by hand. Interrupted daemon starts also leave `cc-daemon-*` folders in the SYSTEM TEMP directory, which the cleanupPeriodDays sweep removes as of v2.1.257 - worth collecting from temp on older versions.  Documented as state to KEEP - not age-swept. daemon.lock can survive a reboot as a stale file (on Windows a stale lock from before the last boot is now replaced, v2.1.257+). | bestätigt | [official](https://code.claude.com/docs/en/agent-view.md) |
| `claude_code.debug_logs` | debug logs | log | Linux, macOS, Windows | `$CLAUDE_CODE_DEBUG_LOGS_DIR/`<br>`%USERPROFILE%\.claude\debug\*.txt`<br>`~/.claude/debug/*.txt`<br>`~/.claude/debug/<session-id>.txt` | text | normal | [EN] Swept under cleanupPeriodDays (default 30 days). Written only while debug logging is on. `claude project purge` deletes per-session entries.  Swept once older than cleanupPeriodDays (default 30 days, minimum 1; 0 is a validation error). `claude project purge` removes per-session debug entries for that project. | bestätigt | [official](https://code.claude.com/docs/en/hooks.md) |
| `claude_code.git_global_excludes` | git global excludes | install_evidence | macOS, Windows, Linux | `$XDG_CONFIG_HOME/git/ignore`<br>`~/.config/git/ignore`<br>`<path from git config core.excludesFile>` | text | normal | [EN] Written once, then persists indefinitely - it is not removed when the settings file it was protecting is deleted. | bestätigt | [official](https://code.claude.com/docs/en/settings.md) |
| `claude_code.install_legacy_and_npm` | install legacy and npm | install_evidence | macOS, Linux, Windows | `~/.claude/local/`<br>`<node-prefix>/lib/node_modules/@anthropic-ai/claude-code`<br>`~/.nvm/versions/node/<version>/lib/node_modules/@anthropic-ai/claude-code`<br>`~/Library/Application Support/Claude/claude-code/<version>/` | binary | normal | [EN] Persist until manually removed (documented removal: rm -rf ~/.claude/local). Multiple installations commonly coexist. | **unbestätigt** | [official](https://code.claude.com/docs/en/troubleshoot-install.md) |
| `claude_code.install_native` | install native | install_evidence | macOS, Linux, Windows | `~/.local/bin/claude`<br>`~/.local/share/claude/versions/<version>`<br>`%USERPROFILE%\.local\bin\claude.exe` | binary | normal | [EN] Auto-update installs new builds under versions/ and repoints the launcher. A CUSTOM script or symlink left at ~/.local/bin/claude is preserved from v2.1.207 onward (earlier versions overwrote it every update), so old versions accumulate under versions/. | bestätigt | [official](https://code.claude.com/docs/en/setup.md) |
| `claude_code.legacy_dirs` | legacy dirs | log | macOS, Windows, Linux | `~/.claude/todos/`<br>`~/.claude/statsig/`<br>`~/.claude/logs/`<br>`%USERPROFILE%\.claude\todos\*`<br>`%USERPROFILE%\.claude\statsig\*`<br>`%USERPROFILE%\.claude\logs\*` | json | normal | [EN] Current versions no longer write these. The retention sweep removes their CONTENTS and then the empty directory - so on a machine that has run a recent version even once, they are likely gone. Their presence dates the evidence to an older installation or a machine whose sweep was paused/skipped. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.legacy_state_dirs` | legacy state dirs | cache | macOS, Windows, Linux | `~/.claude/todos/`<br>`~/.claude/statsig/`<br>`~/.claude/logs/` | json | normal | [EN] No longer written by current versions. The retention sweep removes their CONTENTS and then the empty directory, so they disappear once a current build runs a sweep. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.managed_settings_dropins` | managed settings dropins | permissions | macOS, Windows, Linux | `/Library/Application Support/ClaudeCode/managed-settings.d/*.json`<br>`/etc/claude-code/managed-settings.d/*.json`<br>`C:\Program Files\ClaudeCode\managed-settings.d\*.json` | json | normal | [EN] Same as the managed settings file. | bestätigt | [official](https://code.claude.com/docs/en/managed-settings.md) |
| `claude_code.managed_settings_macos_profile` | managed settings macos profile | permissions | macOS | `/Library/Managed Preferences/com.anthropic.claudecode.plist`<br>`/Library/Managed Preferences/<user>/com.anthropic.claudecode.plist` | binary | normal | [EN] Written by the MDM configuration profile payload; replaced on profile update. | **unbestätigt** | [official](https://code.claude.com/docs/en/managed-settings.md) |
| `claude_code.managed_settings_registry` | managed settings registry | permissions | Windows | `HKLM\SOFTWARE\Policies\ClaudeCode -> value name "Settings"`<br>`HKCU\SOFTWARE\Policies\ClaudeCode -> value name "Settings"` | json | normal | [EN] Registry hive; recoverable from SOFTWARE and NTUSER.DAT in an image, with key LastWrite timestamps. | bestätigt | [official](https://code.claude.com/docs/en/managed-settings.md) |
| `claude_code.org_policy_cache` | org policy cache | config | Linux, macOS, Windows | `%USERPROFILE%\.claude\policy-limits.json`<br>`%USERPROFILE%\.claude\remote-settings.json`<br>`~/.claude/policy-limits.json`<br>`~/.claude/remote-settings.json` | json | normal | [EN] Not age-swept, but BOTH are deleted at logout. remote-settings.json is refreshed at startup and hourly during a session; policy-limits.json refreshes automatically. So content is near-current, and absence may simply mean the user logged out.  DELETED ON LOGOUT (/logout or `claude auth logout`). Not part of the age-based sweep. Checked for updates at startup and hourly during a session; replaced on each successful fetch. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.paste_cache` | paste cache | cache | macOS, Windows, Linux | `~/.claude/paste-cache/`<br>`~/.claude/paste-cache/*`<br>`%USERPROFILE%\.claude\paste-cache\*` | text | normal | [EN] Swept under cleanupPeriodDays (default 30 days). Changelog v2.1.224 notes recalled paste content can have 'aged out', so entries expire independently of the transcript that references them. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.plans` | plans | instructions | macOS, Windows, Linux | `~/.claude/plans/*.md`<br>`%USERPROFILE%\.claude\plans\*.md` | markdown | normal | [EN] Swept under cleanupPeriodDays (default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.plugin_cache` | plugin cache | cache | macOS, Windows, Linux | `~/.claude/plugins/cache/<marketplace>/<plugin>/<version>/` | json | normal | [EN] Versioned: multiple versions of the same plugin can coexist, and orphaned versions are cleaned up over time. Deleting the tree is a documented troubleshooting step (rm -rf ~/.claude/plugins/cache), so absence does not mean a plugin was never installed. | bestätigt | [official](https://code.claude.com/docs/en/plugin-marketplaces.md) |
| `claude_code.plugin_data` | plugin data | cache | macOS, Windows, Linux | `~/.claude/plugins/data/<sanitized-plugin-id>/` | binary | normal | [EN] Plugin-controlled; no documented automatic cleanup. | bestätigt | [official](https://code.claude.com/docs/en/plugins-reference.md) |
| `claude_code.plugin_manifests` | plugin manifests | config | macOS, Windows, Linux | `<plugin-root>/.claude-plugin/plugin.json`<br>`<marketplace-root>/.claude-plugin/marketplace.json`<br>`<plugin-root>/hooks/hooks.json`<br>`<plugin-root>/.mcp.json`<br>`<plugin-root>/.lsp.json`<br>`<plugin-root>/monitors/monitors.json` | json | normal | [EN] Ship with the plugin; versioned inside the plugin cache. | bestätigt | [official](https://code.claude.com/docs/en/plugins-reference.md) |
| `claude_code.plugin_marketplaces_clones` | plugin marketplaces clones | install_evidence | macOS, Windows, Linux | `~/.claude/plugins/marketplaces/<name>/` | json | normal | [EN] Refreshed by /plugin marketplace update. Kept unless removed. CLAUDE_CODE_PLUGIN_KEEP_MARKETPLACE_ON_FAILURE affects retention on failure. | bestätigt | [official](https://code.claude.com/docs/en/plugin-marketplaces.md) |
| `claude_code.plugins_root` | plugins root | install_evidence | macOS, Windows, Linux | `~/.claude/plugins/`<br>`$CLAUDE_CODE_PLUGIN_CACHE_DIR/` | json | normal | [EN] Documented as a directory NOT to delete. Orphaned plugin versions are cleaned up over time. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.plugins_synced` | plugins synced | install_evidence | macOS, Windows, Linux | `~/.claude/plugins/synced/` | json | normal | [EN] Downloaded into the session's own environment; not present in terminal sessions the user starts themselves. | bestätigt | [official](https://code.claude.com/docs/en/plugins-reference.md) |
| `claude_code.project_claude_local_md` | project claude local md | project_instructions | macOS, Windows, Linux | `<project>/CLAUDE.local.md`<br>`<project>/**/CLAUDE.local.md` | markdown | normal | [EN] Created manually and expected to be gitignored, so usually NOT recoverable from version control - disk acquisition is the only source. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.project_claude_md` | project claude md | project_instructions | macOS, Windows, Linux | `<project>/CLAUDE.md`<br>`<project>/.claude/CLAUDE.md`<br>`<project>/**/CLAUDE.md` | markdown | normal | [EN] In the working tree; usually committed, so git history holds prior versions. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.project_mcp_json` | project mcp json | mcp_config | macOS, Windows, Linux | `<project>/.mcp.json` | json | normal | [EN] In the working tree; intended to be committed, so git history holds prior versions. | bestätigt | [official](https://code.claude.com/docs/en/mcp.md) |
| `claude_code.project_rules` | project rules | project_instructions | macOS, Windows, Linux | `<project>/.claude/rules/**/*.md` | markdown | normal | [EN] In the working tree; typically committed. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.project_settings` | project settings | config | macOS, Windows, Linux | `<project>/.claude/settings.json` | json | normal | [EN] Lives in the repository working tree; normally committed to version control, so git history is a second source for prior versions. | bestätigt | [official](https://code.claude.com/docs/en/settings.md) |
| `claude_code.session_env` | session env | config | Linux, macOS, Windows | `%USERPROFILE%\.claude\session-env\*`<br>`~/.claude/session-env/`<br>`~/.claude/session-env/*` | json | normal | [EN] Swept under cleanupPeriodDays (default 30 days).  Swept once older than cleanupPeriodDays (default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.settings_referenced_executables` | settings referenced executables | config | macOS, Windows, Linux | `<operator-defined path referenced from a settings key>`<br>`<project>/.claude/hooks/*`<br>`~/.claude/hooks/*` | text | normal | [EN] Follows whatever file the operator pointed at; not managed or swept by Claude Code. | bestätigt | [official](https://code.claude.com/docs/en/settings-reference.md) |
| `claude_code.skills_trash` | skills trash | instructions | macOS, Windows, Linux | `~/.claude/skills/.trash/` | markdown | normal | [EN] Populated when `syncClaudeAiSkills: false` is set in user or managed settings. No documented automatic purge. | bestätigt | [official](https://code.claude.com/docs/en/settings-reference.md) |
| `claude_code.synced_skills` | synced skills | instructions | macOS, Windows, Linux | `~/.claude/skills/synced/` | markdown | normal | [EN] Downloaded only during a run with CLAUDE_CODE_SYNC_SKILLS set (non-interactive -p mode); the files then persist on disk. Removed on the next sync after the skill is disabled on claude.ai. Setting `syncClaudeAiSkills: false` in user or managed settings MOVES them to ~/.claude/skills/.trash/ instead of deleting. | bestätigt | [official](https://code.claude.com/docs/en/skills.md) |
| `claude_code.uploads` | uploads | cache | macOS, Windows, Linux | `~/.claude/uploads/<session-id>/`<br>`~/.claude/uploads/*/*`<br>`%USERPROFILE%\.claude\uploads\*\*` | binary | normal | [EN] Swept under cleanupPeriodDays (default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.usage_data` | usage data | log | macOS, Windows, Linux | `~/.claude/usage-data/`<br>`~/.claude/usage-data/report.html` | text | normal | [EN] Swept once older than cleanupPeriodDays (default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.usage_reports` | usage reports | log | macOS, Windows, Linux | `~/.claude/usage-data/report.html`<br>`~/.claude/usage-data/*`<br>`%USERPROFILE%\.claude\usage-data\*` | text | normal | [EN] Swept at startup under cleanupPeriodDays (default 30 days). | bestätigt | [official](https://code.claude.com/docs/en/costs.md) |
| `claude_code.worktreeinclude` | worktreeinclude | config | macOS, Windows, Linux | `<project>/.worktreeinclude` | text | normal | [EN] In the working tree. | bestätigt | [official](https://code.claude.com/docs/en/worktrees.md) |
| `claude_code.worktrees` | worktrees | file_snapshot | Linux, macOS, Windows | `<project>/.claude/worktrees/`<br>`<repo-root>/.claude/worktrees/*` | binary | normal | [EN] Orphaned worktrees are removed automatically once older than cleanupPeriodDays. A subagent worktree is cleaned up automatically if the subagent made no changes.  Removed when the background session is deleted, but explicitly KEPT when the worktree holds commits Claude Code cannot confirm are saved elsewhere (the delete is refused and names the branch and unpushed commit count), when another running session holds or locks it, or when removal fails. Orphaned worktrees are removed by the same age cutoff as the retention sweep. v2.1.157 and v2.1.246 fixed the sweep both orphaning and wrongly deleting worktrees, so version matters. | bestätigt | [official](https://code.claude.com/docs/en/worktrees.md) |

### durable

**Meist noch vorhanden.** Vom Aufräumlauf nicht erfasst und überleben damit regelmässig die Transkripte, die sie beschreiben. Sind die Transkripte schon weg, ist diese Gruppe das, was bleibt, und sie genügt oft, um zu belegen, dass ein Agent lief, was er durfte und was gefragt wurde.

| Kennung | Name | Kategorie | Betriebssysteme | Pfade | Format | Sensitivität | Aufbewahrung | Status | Quelle |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `claude_code.agent_memory` | agent memory | memory | Linux, macOS, Windows | `%USERPROFILE%\.claude\agent-memory\*`<br>`<project>/.claude/agent-memory-local/<agent-name>/`<br>`<project>/.claude/agent-memory/<agent-name>/`<br>`<project>/.claude/agent-memory/<agent-name>/MEMORY.md`<br>`~/.claude/agent-memory/<agent-name>/`<br>`~/.claude/agent-memory/<agent-name>/MEMORY.md` | markdown | normal | [EN] Vendor lists `agent-memory/` among the state files to KEEP - not swept by the age-based cleanup. Persists indefinitely until deleted.  Listed by the docs as a state file to KEEP - not removed by the retention sweep. | bestätigt | [official](https://code.claude.com/docs/en/sub-agents.md) |
| `claude_code.agents` | agents | config | macOS, Windows, Linux | `~/.claude/agents/*.md`<br>`<project>/.claude/agents/*.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/sub-agents.md) |
| `claude_code.auto_memory` | auto memory | memory | Linux, macOS, Windows | `%USERPROFILE%\.claude\projects\*\memory\*.md`<br>`~/.claude/projects/<project>/memory/*.md`<br>`~/.claude/projects/<project>/memory/MEMORY.md` | markdown | normal | [EN] EXPLICITLY EXCLUDED from the age-based retention sweep - MEMORY.md and topic files stay until a human or Claude edits or deletes them. The directory itself is removed only if it has been empty for the whole retention period. Before v2.1.228 the sweep treated folders inside the memory directory as session data and could delete old files beneath it. `claude project purge` does delete memory for the named project.  The age-based sweep does NOT delete these files. The memory directory is removed only if it has been empty for the whole retention period. Before v2.1.228 the sweep treated folders inside the memory directory as session data and could delete old files beneath it. `claude project purge <path>` deletes auto memory for that project. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.commands` | commands | instructions | macOS, Windows, Linux | `~/.claude/commands/*.md`<br>`<project>/.claude/commands/*.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.global_config` | global config | config | Linux, macOS, Windows | `$CLAUDE_CONFIG_DIR/.claude.json`<br>`%USERPROFILE%\.claude.json`<br>`~/.claude.json` | json | normal | [EN] Not swept. Rewritten by Claude Code whenever /config options change or trust prompts are accepted; earlier versions are copied into ~/.claude/backups/ (five newest kept).  Not swept; rewritten in place as state changes, with the five previous versions rotating through ~/.claude/backups/. `claude project purge <path>` removes just that project's entry from it. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.history_jsonl` | history jsonl | prompt_history | Linux, macOS, Windows | `$CLAUDE_CONFIG_DIR/history.jsonl`<br>`%USERPROFILE%\.claude\history.jsonl`<br>`~/.claude/history.jsonl` | jsonl | normal | [EN] NOT part of the age-based retention sweep - kept until the user deletes it, so it routinely outlives the transcripts. `claude project purge <path>` filters out matching prompt lines; `claude project purge --all` deletes the file outright. Writing is suppressed entirely by CLAUDE_CODE_SKIP_PROMPT_HISTORY.  NOT swept by the age-based cleanup - kept until deleted. But `claude project purge <path>` filters out the lines typed in that project (the dry-run plan prints e.g. 'filter: /home/alice/.claude/history.jsonl - 12 prompt(s) typed in this project'), and `claude project purge --all` deletes the file outright. Not written at all when CLAUDE_CODE_SKIP_PROMPT_HISTORY=1. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.jobs` | jobs | log | Linux, macOS, Windows | `%USERPROFILE%\.claude\jobs\*\state.json`<br>`~/.claude/jobs/*/state.json`<br>`~/.claude/jobs/*/tmp/*`<br>`~/.claude/jobs/<id>/state.json`<br>`~/.claude/jobs/<id>/tmp/` | json | normal | [EN] Documented as state to KEEP - not age-swept. The per-session tmp/ directory is removed when the session is deleted.  Listed by vendor among the state files to KEEP (not swept by the age-based cleanup of transcripts), but a job's `tmp/` directory is removed when the session is deleted, and background-session records are subject to a 30-day job retention sweep (changelog v2.1.157/v2.1.246 discuss that sweep removing or sparing worktrees). Deleting a session with `claude rm <id>` does NOT delete its transcript, which stays available via `claude --resume`. | bestätigt | [official](https://code.claude.com/docs/en/agent-view.md) |
| `claude_code.keybindings` | keybindings | config | macOS, Windows, Linux | `~/.claude/keybindings.json` | json | normal | [EN] Not swept. Read at session start and hot-reloaded when edited. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.known_marketplaces` | known marketplaces | config | macOS, Windows, Linux | `~/.claude/plugins/known_marketplaces.json`<br>`$CLAUDE_CODE_PLUGIN_CACHE_DIR/known_marketplaces.json` | json | normal | [EN] Written by /plugin marketplace add and the claude plugin commands; not swept. Marketplace state is stored once per user, not per project. | bestätigt | [official](https://code.claude.com/docs/en/plugin-marketplaces.md) |
| `claude_code.loop_instructions` | loop instructions | instructions | macOS, Windows, Linux | `~/.claude/loop.md`<br>`<project>/.claude/loop.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/scheduled-tasks.md) |
| `claude_code.managed_claude_md` | managed claude md | instructions | macOS, Windows, Linux | `/Library/Application Support/ClaudeCode/CLAUDE.md`<br>`/etc/claude-code/CLAUDE.md`<br>`C:\Program Files\ClaudeCode\CLAUDE.md` | markdown | normal | [EN] Deployed by IT; not swept. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.managed_mcp_json` | managed mcp json | mcp_config | macOS, Linux, Windows | `/Library/Application Support/ClaudeCode/managed-mcp.json`<br>`/etc/claude-code/managed-mcp.json`<br>`C:\Program Files\ClaudeCode\managed-mcp.json` | json | normal | [EN] Deployed by IT; not swept. | bestätigt | [official](https://code.claude.com/docs/en/managed-mcp.md) |
| `claude_code.managed_settings_file` | managed settings file | permissions | macOS, Windows, Linux | `/Library/Application Support/ClaudeCode/managed-settings.json`<br>`/etc/claude-code/managed-settings.json`<br>`C:\Program Files\ClaudeCode\managed-settings.json`<br>`C:\ProgramData\ClaudeCode\managed-settings.json` | json | normal | [EN] Deployed by MDM/config management; read at startup and reloaded when the file changes. Not swept by Claude Code. | bestätigt | [official](https://code.claude.com/docs/en/managed-settings.md) |
| `claude_code.mcp_logs` | mcp logs | log | macOS, Windows, Linux | `~/Library/Caches/claude-cli-nodejs/<encoded-cwd>/mcp-logs-<server>/<timestamp>.jsonl`<br>`%LOCALAPPDATA%\claude-cli-nodejs\Cache\<encoded-cwd>\mcp-logs-<server>\<timestamp>.jsonl`<br>`$XDG_CACHE_HOME/claude-cli-nodejs/<encoded-cwd>/mcp-logs-<server>/<timestamp>.jsonl`<br>`~/.cache/claude-cli-nodejs/<encoded-cwd>/mcp-logs-<server>/<timestamp>.jsonl` | jsonl | normal | [EN] Reported to accumulate without bound - one user reported 472GB across ~/.claude/debug and this tree over 4-5 months of use. cleanupPeriodDays does not reliably reach this cache, so logs here can far outlive transcripts and prompt history. | **unbestätigt** | [community](https://github.com/anthropics/claude-code/issues/13679) |
| `claude_code.output_styles` | output styles | instructions | macOS, Windows, Linux | `~/.claude/output-styles/*.md`<br>`<project>/.claude/output-styles/*.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.project_settings_local` | project settings local | permissions | macOS, Windows, Linux | `<project>/.claude/settings.local.json` | json | normal | [EN] Not swept. Persists until deleted. Written incrementally as the user grants standing approvals. | bestätigt | [official](https://code.claude.com/docs/en/settings.md) |
| `claude_code.shell_profile_evidence` | shell profile evidence | install_evidence | macOS, Linux | `~/.zshrc`<br>`$ZDOTDIR/.zshrc`<br>`~/.bashrc`<br>`~/.bash_profile`<br>`~/.bash_login`<br>`~/.profile`<br>`~/.config/fish/config.fish` | text | normal | [EN] User-maintained; persists until edited. | bestätigt | [official](https://code.claude.com/docs/en/troubleshoot-install.md) |
| `claude_code.skills` | skills | instructions | macOS, Windows, Linux | `~/.claude/skills/<skill-name>/SKILL.md`<br>`<project>/.claude/skills/<skill-name>/SKILL.md`<br>`/Library/Application Support/ClaudeCode/.claude/skills/<skill-name>/SKILL.md`<br>`/etc/claude-code/.claude/skills/<skill-name>/SKILL.md`<br>`C:\Program Files\ClaudeCode\.claude\skills\<skill-name>\SKILL.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/skills.md) |
| `claude_code.stats_cache` | stats cache | cache | Linux, macOS, Windows | `%USERPROFILE%\.claude\stats-cache.json`<br>`~/.claude/stats-cache.json` | json | normal | [EN] NOT swept - kept until deleted. Survives the 30-day transcript sweep, so it can evidence activity whose transcripts are gone.  NOT age-swept - kept until the user deletes it, so it typically outlives the transcripts it was derived from. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.task_lists` | task lists | memory | macOS, Windows, Linux | `~/.claude/tasks/`<br>`~/.claude/tasks/*/*`<br>`%USERPROFILE%\.claude\tasks\*\*` | json | normal | [EN] Brought under the cleanupPeriodDays sweep in v2.1.117 (before that it was not swept, so older evidence may hold task lists far beyond 30 days). `claude project purge` deletes per-session entries. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory) |
| `claude_code.themes` | themes | config | macOS, Windows, Linux | `~/.claude/themes/*.json` | json | normal | [EN] Not swept. Read at session start and hot-reloaded when files change. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.user_claude_md` | user claude md | instructions | macOS, Windows, Linux | `~/.claude/CLAUDE.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/memory.md) |
| `claude_code.user_rules` | user rules | instructions | macOS, Windows, Linux | `~/.claude/rules/**/*.md` | markdown | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |
| `claude_code.user_settings` | user settings | config | macOS, Windows, Linux | `~/.claude/settings.json`<br>`$CLAUDE_CONFIG_DIR/settings.json` | json | normal | [EN] Never swept. Persists until the user deletes it. Claude Code writes it the first time a /config option that belongs to user scope changes (e.g. theme). | bestätigt | [official](https://code.claude.com/docs/en/settings.md) |
| `claude_code.user_settings_local` | user settings local | permissions | macOS, Windows, Linux | `~/.claude/settings.local.json` | json | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/permissions.md) |
| `claude_code.workflows` | workflows | config | macOS, Windows, Linux | `~/.claude/workflows/*.js`<br>`<project>/.claude/workflows/*.js` | text | normal | [EN] Not swept. | bestätigt | [official](https://code.claude.com/docs/en/claude-directory.md) |

### Veraltete Pfade

Die folgenden Pfade werden von aktuellen Versionen nicht mehr geschrieben oder gelesen. Sie werden trotzdem gesammelt, weil sie eine ältere Installation belegen, dürfen aber nie als aktueller Zustand dargestellt werden.

- `claude_code.legacy_state_dirs`: `~/.claude/todos/`, `~/.claude/statsig/`, `~/.claude/logs/`
